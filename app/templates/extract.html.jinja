{% extends "base.html.jinja" %}
{% block content %}

<h2 class="mt-3 mb-3">Ekstrakcja opinii</h2>

<form method="POST" action="{{url_for('extract')}}" class="container">
   
    <div class="row 3-g align-items-center"></div>

        <div class="col-auto">
            <label for="product_id" class="col-form-label">Kod produktu z Ceneo.pl</label>
        </div>
        
        <div class="col-auto">
            <input type="text" name="product_id" class="form-control" id="product_id" aria-describedby="product_idHelpInline" placeholder="Podaj kod poduktu">
        </div>

        <div class="col-auto">
            <span id="product_idHelpInline" class="form-text">Kod produktu musi zawierać 5-10 cyfr.</span>
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-dark">Pobierz opinie</button>
        </div>

    </div>

</form>

{% endblock content %}